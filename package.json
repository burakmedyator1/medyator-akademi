{
  "name": "medyator-akademi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "npm start --prefix backend",
    "seed": "npm run seed --prefix backend"
  }
}
